name: mitproxy_val
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  get:
  http:
  shimmer: ^3.0.0
  connectivity_plus:
  shared_preferences: any
  dash_chat_2: ^0.0.19
  cupertino_icons: ^1.0.2
  flutter_dotenv: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/val_logo_red.png
    - lib/.env